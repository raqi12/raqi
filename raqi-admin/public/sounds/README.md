# Notification sounds for the admin dashboard bell

Drop your file here as one of:

- `notification.wav` (default chime ships with the repo)
- `notification.mp3`
- `notification.ogg`

The first real audio file wins (HTML SPA fallbacks are ignored). After replacing the file, hard-refresh the admin app.
